import AppShell from "@/components/AppShell";
import EmptyState from "@/components/EmptyState";
import AuthPanel from "@/components/AuthPanel";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { ArrowRight, Bell, CheckCircle2, Gift, ShieldCheck, Sparkles, Target } from "lucide-react";
import { Link } from "wouter";

export default function Home() {
  // Managed preview HMR smoke check: this render path remains side-effect free.
  const { isAuthenticated, user } = useAuth();
  const params = typeof window !== "undefined" ? new URLSearchParams(window.location.search) : null;
  const authRetry = params?.get("auth") === "retry";
  const oauthDenied = params?.get("oauth") === "denied";
  const summary = trpc.dashboard.summary.useQuery(undefined, { enabled: isAuthenticated });
  const tasksQuery = trpc.tasks.list.useQuery(undefined, { enabled: isAuthenticated });
  if (!isAuthenticated) return <AppShell title="6lory" eyebrow="Doğrulanmış kazanım">{oauthDenied && <div role="alert" className="mb-5 rounded-2xl border border-amber-500/25 bg-amber-500/10 px-4 py-3 text-sm leading-6 text-amber-900 dark:text-amber-200">Google/YouTube izni verilmedi. İsterseniz daha sonra aynı düğmeden güvenli giriş ve YouTube doğrulamasını yeniden başlatabilirsiniz.</div>}{authRetry && <div role="alert" className="mb-5 rounded-2xl border border-amber-500/25 bg-amber-500/10 px-4 py-3 text-sm leading-6 text-amber-900 dark:text-amber-200">Güvenli giriş oturumu doğrulanamadı. Çerezleri etkinleştirip aşağıdaki düğmeden yeni bir giriş başlatın.</div>}<section className="grid gap-8 py-6 lg:grid-cols-[1.25fr_0.75fr] lg:py-14"><div><span className="inline-flex items-center gap-2 rounded-full border border-teal-600/20 bg-teal-500/10 px-3 py-1.5 text-xs font-bold text-teal-700 dark:text-teal-300"><ShieldCheck className="size-3.5" /> Güven merkezli görev platformu</span><h1 className="mt-5 max-w-3xl font-display text-4xl font-bold leading-[1.05] tracking-[-0.04em] sm:text-6xl">Her kazanımın arkasında <span className="text-teal-700 dark:text-teal-300">doğrulanmış</span> bir adım olsun.</h1><p className="mt-6 max-w-xl text-base leading-7 text-muted-foreground">6lory; görev, doğrulama, puan ve ödül akışını tek bir sade deneyimde birleştirir. Puanlar yalnızca sunucuda doğrulanan süreçlerden sonra oluşur.</p><div className="mt-8 flex flex-wrap gap-3"><a href="#auth" className="inline-flex items-center rounded-2xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground shadow-sm">Manuel giriş yap <ArrowRight className="ml-2 size-4" /></a><Link href="/tasks" className="inline-flex items-center rounded-2xl border border-border bg-card/70 px-5 text-sm font-semibold shadow-sm">Görev sistemini incele</Link></div><div className="mt-10 flex flex-wrap gap-x-8 gap-y-4 text-sm text-muted-foreground"><span className="flex items-center gap-2"><CheckCircle2 className="size-4 text-teal-600 dark:text-teal-300" /> Sunucu taraflı puan ledger’ı</span><span className="flex items-center gap-2"><CheckCircle2 className="size-4 text-teal-600 dark:text-teal-300" /> Şeffaf doğrulama sonucu</span></div></div><div className="rounded-[2rem] border border-white/70 bg-card/70 p-5 shadow-2xl shadow-slate-950/8 backdrop-blur-xl dark:border-white/10 sm:p-7"><div className="flex items-center justify-between"><span className="text-xs font-bold uppercase tracking-[0.14em] text-muted-foreground">Doğrulama zinciri</span><span className="rounded-full bg-teal-500/10 px-2.5 py-1 text-[11px] font-bold text-teal-700 dark:text-teal-300">Şeffaf</span></div><div className="mt-7 space-y-3">{[["1", "Görevi başlat", "Süreli ve kullanıcıya bağlı oturum"], ["2", "Sinyalleri değerlendir", "Doğrulanabilir veri veya manuel inceleme"], ["3", "Ledger kaydı oluştur", "Tekil ve değiştirilemez puan işlemi"]].map(([number, title, detail]) => <div key={number} className="flex gap-3 rounded-2xl bg-muted/70 p-3.5"><span className="grid size-7 shrink-0 place-items-center rounded-lg bg-slate-950 text-xs font-bold text-white dark:bg-teal-300 dark:text-slate-950">{number}</span><div><p className="text-sm font-bold">{title}</p><p className="mt-0.5 text-xs leading-5 text-muted-foreground">{detail}</p></div></div>)}</div></div></section><div id="auth" className="mt-8 scroll-mt-24"><AuthPanel /></div></AppShell>;
  const balance = summary.data?.balance;
  return <AppShell title="Ana Sayfa" eyebrow="Hoş geldiniz"><section className="rounded-[2rem] bg-[linear-gradient(135deg,oklch(0.23_0.035_245),oklch(0.31_0.07_195))] p-6 text-white shadow-xl shadow-slate-950/10 sm:p-8"><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="text-sm text-white/65">Merhaba, {user?.name?.split(" ")[0] || "6lory kullanıcısı"}</p><h1 className="mt-1 font-display text-3xl font-bold tracking-tight">Kazanımınız, güvenle ilerliyor.</h1></div><span className="rounded-2xl bg-white/10 px-4 py-3 text-right backdrop-blur"><span className="block text-[10px] font-bold uppercase tracking-[0.14em] text-white/60">Kullanılabilir bakiye</span><span className="mt-1 block font-display text-2xl font-bold">{new Intl.NumberFormat("tr-TR").format(balance?.availablePoints ?? 0)} <small className="text-sm font-semibold text-white/65">puan</small></span></span></div><div className="mt-7 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-white/10 p-3.5"><p className="text-[11px] font-bold uppercase tracking-wider text-white/60">Toplam kazanım</p><p className="mt-1 font-display text-xl font-bold">{new Intl.NumberFormat("tr-TR").format(balance?.lifetimeEarned ?? 0)}</p></div><div className="rounded-2xl bg-white/10 p-3.5"><p className="text-[11px] font-bold uppercase tracking-wider text-white/60">Tamamlanan</p><p className="mt-1 font-display text-xl font-bold">{summary.data?.completedTasks ?? 0} görev</p></div><div className="rounded-2xl bg-white/10 p-3.5"><p className="text-[11px] font-bold uppercase tracking-wider text-white/60">Bildirim</p><p className="mt-1 font-display text-xl font-bold">{summary.data?.unreadNotifications ?? 0} yeni</p></div></div></section><section className="mt-8"><div className="mb-4 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-teal-700 dark:text-teal-300">Size özel</p><h2 className="mt-1 font-display text-2xl font-bold">Önerilen görevler</h2></div><Link href="/tasks" className="text-sm font-semibold text-teal-700 dark:text-teal-300">Tümünü gör</Link></div>{tasksQuery.isLoading ? <div className="grid gap-4 md:grid-cols-2">{[1,2].map(i => <div key={i} className="h-40 animate-pulse rounded-3xl bg-muted" />)}</div> : !tasksQuery.data?.length ? <EmptyState icon={Target} title="Yeni görevler için hazırız" description="Yönetici görev yayınladığında uygunluk kontrollerinizden sonra burada görünecek." /> : <div className="grid gap-4 md:grid-cols-2">{tasksQuery.data.slice(0, 2).map(task => <Link key={task.id} href="/tasks" className="rounded-3xl border border-border/80 bg-card/75 p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex items-start justify-between"><span className="rounded-lg bg-teal-500/10 px-2 py-1 text-[11px] font-bold text-teal-700 dark:text-teal-300">{task.platform}</span><span className="text-sm font-bold text-teal-700 dark:text-teal-300">+{task.rewardPoints}</span></div><h3 className="mt-4 font-display text-lg font-bold">{task.title}</h3><p className="mt-2 line-clamp-2 text-sm text-muted-foreground">{task.description}</p></Link>)}</div>}</section><section className="mt-8 grid gap-4 md:grid-cols-2"><Link href="/rewards" className="rounded-3xl border border-border/80 bg-card/75 p-5 shadow-sm"><Gift className="size-5 text-teal-700 dark:text-teal-300" /><h3 className="mt-4 font-display text-lg font-bold">Ödüller</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">Puanlarınızı gerçek stok ve limit kontrolleri ile değerlendirin.</p></Link><Link href="/notifications" className="rounded-3xl border border-border/80 bg-card/75 p-5 shadow-sm"><Bell className="size-5 text-teal-700 dark:text-teal-300" /><h3 className="mt-4 font-display text-lg font-bold">Bildirim merkezi</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">Görev, doğrulama ve ödül süreçlerini tek yerden takip edin.</p></Link></section></AppShell>;
}
